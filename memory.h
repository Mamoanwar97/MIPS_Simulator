#ifndef MEMORY_H
#define MEMORY_H
#include <bits/stdc++.h>
#include <QObject>

class Memory : public QObject
{
    Q_OBJECT

private:
    map<
public:
    explicit Memory(QObject *parent = nullptr);

signals:

public slots:
};

#endif // MEMORY_H
